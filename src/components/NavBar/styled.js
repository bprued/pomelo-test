import styled from 'styled-components'

export const NavBarStyled = styled.div`
  .menu {
    height: 5rem;
    align-items: center;
    font-size: 1.5rem;
    .item {
      padding: 1rem 0.3rem;
    }
    button {
      background: none;
      color: black;
    }
    img {
      cursor: pointer;
    }
  }
`